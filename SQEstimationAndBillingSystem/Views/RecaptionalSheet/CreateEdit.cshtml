@model SQEstimationAndBillingSystem.Models.RecaptionalModel
@{
    ViewBag.Title = "CreateEdit";
}

<section class="content-header">
    <h1>
        Add/Update Recaptional
    </h1>
</section>
<div class="row">
    <div class="col-xs-12" style="text-align:right; margin:5px">
        <input type="button" style="margin-right:21px; width:64px;" value="Back" onclick="BackToIndex()" />
    </div>

</div>
@*@using (Html.BeginForm("AddEditRecaptional", "RecaptionalSheet", FormMethod.Post, new { id = "frmChild" }))
{
    <div class="panel panel-default">
        <div class="panel-body text-right">
            @Html.HiddenFor(model => model.id)
            <div class="row">
                <div class="col-sm-3">
                    @Html.EditorFor(model => model.Chapter, new { htmlAttributes = new { @class = "form-control", @maxlength = "50", @style = "text-align:center" } })
                    @Html.ValidationMessageFor(model => model.Chapter, "", new { @class = "text-danger" })
                </div>
            </div>

        </div>
        <div class="form-group row">
            <div class="col-sm-12" style="text-align:right">
                <input type="submit" value="Submit" style="margin-right: 15px;" class="btnbtn-default mybtn">
            </div>
        </div>
    </div>

}*@
<script>
    function BackToIndex() {

        //   var confirmation = confirm("Your changes will be discarded.");
        var confirmation = false;
        bootbox.confirm({
            title: "Confirmation",
            message: "Your changes will be discarded! Are you sure?",
            buttons: {
                confirm: {
                    label: 'Yes',
                    className: ' mybtn bootboxbutton'
                },
                cancel: {
                    label: 'No',
                    className: ' cancelbtn bootboxbutton'
                }
            },
            callback: function (confirmation) {
                if (confirmation) {
                    document.location.href = "/RecaptionalSheet/Index";
                }
            }
        })
    }
</script>

